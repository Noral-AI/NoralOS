{
  "name": "@noralos-plugins/voice-cascade",
  "version": "0.1.0",
  "description": "TTS execution plugin for NoralOS. Reads voice-config for eligibility, runs pre-TTS exfiltration scan, then synthesizes audio via ElevenLabs or Google Cloud TTS with serial fallback. Defaults to dry_run; live mode requires explicit opt-in.",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "noralosPlugin": {
    "manifest": "./dist/manifest.js",
    "worker": "./dist/worker.js"
  },
  "scripts": {
    "prebuild": "pnpm --filter @noralos/plugin-sdk ensure-build-deps",
    "build": "tsc",
    "clean": "rm -rf dist",
    "typecheck": "pnpm --filter @noralos/plugin-sdk ensure-build-deps && tsc --noEmit"
  },
  "dependencies": {
    "@noralos/plugin-sdk": "workspace:*",
    "@noralos/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "typescript": "^5.7.3"
  }
}
