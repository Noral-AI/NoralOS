/**
 * NoralSign UI bundle entrypoint.
 *
 * The host loads each named export by `exportName` declared in the
 * manifest's `ui.slots` block. Keep this file as a re-export hub —
 * substance lives in the per-component files.
 */

export { NoralSignSidebarLink } from "./NoralSignSidebarLink.js";
export { NoralSignTemplatesPage } from "./NoralSignTemplatesPage.js";
